{
  "name": "crosspoint-pxc-converter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run --passWithNoTests",
    "gen:blue-noise": "tsx scripts/gen-blue-noise.ts"
  },
  "dependencies": {
    "pica": "^9.0.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.10",
    "vitest": "^3.2.4"
  }
}
